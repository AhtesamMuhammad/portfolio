---
interface Props {
  title: string;
  date: string;
  description: string;
  company: string;
}

const { title, date, description, company } = Astro.props;
---

<li class="mb-4 md:mb-10 ms-6 flex gap-4 flex-col md:flex-row">
  <div class="flex-none w-44">
    <div
      class="absolute w-3 h-3 bg-orange-400 rounded-full mt-1.5 -start-1.5 border border-white"
    >
    </div>
    <h3 class="text-lg leading-5 font-semibold text-orange-400 ">
      {title}
    </h3>
    <p class="text-lg font-medium text-gray-400 ">
      {company}
    </p>
    <time
      class="mb-1 text-sm font-normal leading-none text-gray-400 "
      >{date}</time
    >
  </div>
  <p class="flex-1 mb-4 text-sm font-normal text-gray-500 text-pretty">
    {description}
  </p>
</li>
